const express = require('express');
const router = express.Router();
const { protect } = require('../middleware/authMiddleware');

// You can add user-specific routes here as you develop more features

module.exports = router;